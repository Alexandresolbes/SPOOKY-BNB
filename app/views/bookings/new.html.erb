<%= @listing.title %>

<%= simple_form_for [@listing, @booking] do |f| %>
  <%= f.input :start_date %>
  <%= f.input :end_date %>
  <%= f.submit "Create a booking" %>
<% end %>

<%= link_to 'Back', listing_path(@listing) %>
