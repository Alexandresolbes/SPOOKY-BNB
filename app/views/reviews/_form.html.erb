<%= simple_form_for [listing, booking, @review] do |f| %>
  <%= f.input :title %>
  <%= f.input :content %>
  <%= f.input :rating, collection: 1..5 %>
  <div class="text-center">
    <%= f.submit "Post", class: "btn btn-green mt-3" %>
  </div>
<% end %>
